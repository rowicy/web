---
interface Props {
  isBg?: boolean;
}

const { isBg } = Astro.props;
---

<section
  class=`pt-8 mx-[calc(((100vw-100%)/2)*-1)] px-[calc((100vw-100%)/2)] ${isBg && 'pb-8 bg-primary text-white'}`
>
  <slot />
</section>
