---
const { title } = Astro.props;
---

<section
  class="bg-primary p-10 flex justify-center items-center h-[100svh] mx-[calc(((100vw-100%)/2)*-1)] px-[calc((100vw-100%)/2)]"
>
  <h1 class="font-bold text-white text-3xl md:text-5xl">{title}</h1>
</section>
